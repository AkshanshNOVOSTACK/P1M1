<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.factor8.p1m1">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".View.BackgroundProcessesActivity" />
        <activity android:name=".LoginRegistration.Login2Activity" />
        <activity android:name=".LoginRegistration.LoginActivity" />
        <activity android:name=".View.Goals.SetGoalActivity" />
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".View.Goals.EditProfileActivity" />
        <activity android:name=".View.Goals.AutomobileActivity" />
        <activity android:name=".View.Goals.ElectronicsActivity" />
        <activity android:name=".View.Goals.HolidaysActivity" />
        <activity android:name=".View.Goals.PersonalSavingsActivity" />

        <service android:name=".BackgroundService" />
    </application>

</manifest>